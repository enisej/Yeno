import React from 'react';
import {Alert} from "react-bootstrap";

const NotFound = () => {
    return (

        <Alert variant="info" className="mt-5 d-flex justify-content-center" >
           <h1> Nav datu!</h1>
        </Alert>
    );
};

export default NotFound;